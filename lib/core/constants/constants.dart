import 'package:flutter/material.dart';

class AppConstants {
  static const String appName = 'WarungPro';
  static const String dbName = 'posmobile.db';

  // Primary Palette — Indigo Blue (Modern & Semi-Formal)
  static const Color primaryColor = Color(0xFF1A56DB);      // Indigo Blue
  static const Color primaryDarkColor = Color(0xFF1044B5);  // Deep Indigo
  static const Color primaryLightColor = Color(0xFF3B82F6); // Sky Blue
  static const Color accentColor = Color(0xFF4F77F5);
  static const Color successColor = Color(0xFF059669); // Emerald 600
  static const Color warningColor = Color(0xFFD97706); // Amber 600
  static const Color errorColor = Color(0xFFDC2626);   // Red 600

  // Neutral Colors
  static const Color backgroundColor = Color(0xFFF8FAFC); // Slate 50
  static const Color cardBackgroundColor = Colors.white;
  static const Color textDarkColor = Color(0xFF0F172A);    // Slate 950
  static const Color textLightColor = Color(0xFF64748B);   // Slate 500
  static const Color borderLightColor = Color(0xFFE2E8F0); // Slate 200

  // Spacing
  static const double paddingXs = 4.0;
  static const double paddingSm = 8.0;
  static const double paddingMd = 16.0;
  static const double paddingLg = 24.0;
  static const double paddingXl = 32.0;

  // Border Radius — formal & clean
  static const double radiusSm = 6.0;
  static const double radiusMd = 12.0;
  static const double radiusLg = 20.0;
  static const double radiusXl = 28.0;
}
